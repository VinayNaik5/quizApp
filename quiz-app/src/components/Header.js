import React from "react";
import { Toolbar, AppBar } from "@mui/material";

const Header = () =>{
    return (
        <AppBar>
            <Toolbar>Quiz App</Toolbar>
        </AppBar>
        
    )
}
export default Header;